# خطة تنفيذ مشروع متجر مواد الطباعة (NestJS + Docker)

مصدر التوصيف: `توصيف_مشروع_متجر_مواد_الطباعة_NestJS_Docker.docx`

## القاعدة الذهبية للتنفيذ

**كل مهمة تُنفذ يجب أن:**
1. تُبنى بشكل كامل (كود + migration إن وجد + test).
2. تُشغَّل محليًا عبر `docker compose up` (أو الخدمة المعنية) للتأكد أنها تعمل فعليًا، مش بس compile.
3. تُختبر يدويًا (طلب API حقيقي عبر curl/Postman/Swagger) + `npm run test` / `test:e2e` حسب الحالة.
4. لا تُعتبر "خلصانة" ولا ننتقل للمهمة التالية إلا بعد التحقق الفعلي أنها شغالة (لا كود بدون تجربة).
5. تُعمل commit مستقل بعد كل مهمة مكتملة ومُختبرة (رسائل commit واضحة).

---

## قرارات يجب حسمها قبل البدء (من القسم 25 بالتوصيف)

هاي القرارات بتأثر على تصميم قاعدة البيانات من أول فيز، لازم نحسمها قبل أو خلال Phase 1:

- [ ] هل الأسعار تشمل الضريبة أم تُضاف لاحقًا؟
- [ ] هل البيع للأفراد والجملة معًا (نفس الحساب) أم منفصل؟
- [ ] هل المخزون موزع على أكثر من مستودع من البداية؟
- [ ] متى يُحجز المخزون: عند إضافة للسلة، عند إنشاء الطلب، أم عند نجاح الدفع؟
- [ ] هل يُسمح بالبيع بأجزاء من المتر/الرول (كميات عشرية)؟
- [ ] ما بوابات الدفع وشركات الشحن المستهدفة فعليًا؟
- [ ] هل السعر موحّد أم يختلف حسب مجموعة العميل/الفرع؟
- [ ] هل يوجد شحن فقط أم استلام من الفرع أيضًا؟

> سنبدأ بافتراضات معقولة (مستودع واحد، حجز عند تأكيد الطلب، دون ضريبة منفصلة الآن) ونعدلها متى تم الحسم، لكن الأفضل حسمها بأقرب وقت.

---

## Phase 0 — تهيئة البنية التحتية والأمن الأساسي ✅ (مكتملة ومُختبرة محليًا)

- [x] إعداد `docker-compose.yml`: postgres, redis, minio, mailpit (dev فقط), nginx.
  - اختبار: `docker compose up -d` — كل الخدمات الست شغالة (`postgres`, `redis` healthy فعليًا؛ `minio`, `mailpit`, `nginx`, `api` تم التحقق من استجابتها عبر curl).
- [x] Dockerfile متعدد المراحل (base/deps/prod-deps/development/build/production) + مستخدم non-root (`node`) + `.dockerignore`.
  - اختبار: بُنيت صورة `production` وشُغّلت فعليًا متصلة بقاعدة بيانات حقيقية، healthcheck ظهر `healthy`، و`whoami` داخل الحاوية أكّد أنها تعمل بمستخدم `node` لا `root`.
- [x] إعداد Prisma 7 (schema + `prisma.config.ts` + `@prisma/adapter-pg`) + اتصال بقاعدة postgres عبر docker.
  - اختبار: `PrismaService` اتصل فعليًا بالقاعدة (ظهر بالـ logs "Connected to database")، و`/health/ready` أكّد الاتصال حيًا. توليد جداول حقيقية سيُختبر مع أول migration بـ Phase 1.
- [x] `config/` مع `validation.schema.ts` (Joi) لمنع الإقلاع بدون متغيرات بيئة صحيحة.
- [x] Helmet, CORS allow-list, تعطيل X-Powered-By, ValidationPipe (whitelist + forbidNonWhitelisted + transform) عام على مستوى `main.ts`.
  - اختبار: تأكدنا من ظهور رؤوس Helmet الأمنية فعليًا بالاستجابة (CSP, HSTS, X-Frame-Options...) وغياب X-Powered-By. اختبار forbidNonWhitelisted الكامل هيصير مع أول DTO حقيقي بـ Phase 1.
- [x] `/health/live` و`/health/ready` (فحص اتصال DB/Redis فعليًا عبر Terminus).
  - اختبار: `curl` مباشر وعبر nginx reverse proxy رجعوا 200 مع تفاصيل صحة DB وRedis.
- [x] Pino structured logging (JSON بالإنتاج، pretty بالتطوير، redact لكلمات المرور والتوكنات).
  - اختبار: راجعنا سطور log فعلية (dev وproduction)، ولقينا وصلّحنا باگ حقيقي: كانت الصورة رح تنهار بالإنتاج لو NODE_ENV اتضبط غلط لأن `pino-pretty` (devDependency) مو موجودة بصورة الإنتاج — تم عمل fallback آمن.

**ملاحظات من التنفيذ الفعلي (تستحق التوثيق):**
- Prisma 7 غيّر الـ API: الاتصال بقاعدة البيانات صار عبر `adapter` (`@prisma/adapter-pg`) مو عبر `datasource.url` بالـ schema مباشرة.
- تم تصحيح `tsconfig.build.json` (`rootDir: src` + استثناء `prisma.config.ts`) لأن وجود `prisma.config.ts` بجذر المشروع كان يغيّر مسار الـ build الناتج (`dist/src/main.js` بدل `dist/main.js`).
- ملف `*.tsbuildinfo` تمت إضافته لـ `.gitignore` و`.dockerignore` لأنه كان يسبب cache قديم فاسد داخل صورة Docker.
- خدمة `worker` (BullMQ) لم تُضف بعد لـ `docker-compose.yml` لأنه لا توجد jobs فعلية حتى الآن — ستُضاف مع Phase 3.

## Phase 1 — الأساس: الهوية، RBAC، الأصناف والصفات ✅ (مكتملة ومُختبرة محليًا)

- [x] Module `users`: تسجيل، تسجيل دخول، تشفير كلمة المرور بـ Argon2id (`@node-rs/argon2`).
  - اختبار: تسجيل مستخدم فعلي عبر curl، تأكدنا أن الباسورد بقاعدة البيانات مشفر (Argon2id) لا نص صريح، ومحاولة تسجيل نفس الإيميل مرتين رجعت 409.
- [x] JWT Access Token (15 دقيقة) + Refresh Token (7 أيام، مخزّن كـ SHA-256 hash بقاعدة البيانات) مع تدوير (rotation) وكشف إعادة الاستخدام (reuse detection).
  - اختبار فعلي: تسجيل دخول → refresh (نجح) → إعادة استخدام الـ refresh القديم (رُفض 401) → التوكن الجديد الناتج عن آخر rotation صار *أيضًا* مرفوض تلقائيًا لأن reuse detection ألغى كل جلسات المستخدم فورًا (حماية من سرقة التوكن).
- [x] إلغاء جلسة واحدة (`/auth/logout`) / كل الجلسات (`/auth/logout-all`) + تسجيل device/IP/userAgent لكل refresh token (كل refresh token = جلسة).
- [x] Rate limiting (`@nestjs/throttler`): عام 100 طلب/دقيقة، ومشدد 5 طلبات/دقيقة على `/auth/login`, `/auth/forgot-password`, `/auth/reset-password`.
  - اختبار: 7 محاولات دخول متتالية بكلمة سر خاطئة → رجعت 429 بعد تجاوز الحد فعليًا.
- [x] Guards + RBAC موحّدة: `JwtAuthGuard` (عام على مستوى التطبيق عبر `APP_GUARD` مع `@Public()` للاستثناء) + `PermissionsGuard` مع `@RequirePermissions(...)`. 26 صلاحية دقيقة موزّعة على 6 أدوار.
  - اختبار: عميل (`customer`) حاول إنشاء صنف → 403 فعليًا. أدمن (`super_admin`) نفس الطلب → 201.
- [ ] MFA لحسابات الإدارة — **لم تُنفذ بعد**، أُجّلت بالكامل (لا حتى الأساس/schema) لأنها تحتاج قرار على آلية TOTP والوقت لم يسمح؛ يجب إضافتها قبل اعتماد النظام بالإنتاج الفعلي كما يوصي التوصيف.
- [x] استعادة كلمة المرور: رمز أحادي الاستخدام (SHA-256 hash، صلاحية 30 دقيقة)، **يُرسل فعليًا عبر بريد حقيقي (Mailpit + nodemailer)** لا يُرجَع بالـ API response (كان اكتشفنا هالثغرة أثناء البناء وصلّحناها فورًا — إرجاع التوكن بالـ response كان بيلغي الفايدة الأمنية من "نسيت كلمة المرور").
  - اختبار كامل فعلي: forgot-password → تحقق من وصول الإيميل عبر Mailpit API → استخدام التوكن لتغيير كلمة المرور → إعادة استخدام نفس التوكن رُفضت (400) → الدخول بكلمة المرور القديمة رُفض (401) → الدخول بالجديدة نجح (200).
- [x] Module `categories`: شجرة أصناف متعددة المستويات (`parentId` + materialized `path`)، CRUD كامل عبر API بدون تعديل شيفرة، دعم نقل صنف (تغيير `parentId`) مع تحديث تلقائي لمسارات كل الأبناء، ومنع الحلقات (نقل صنف تحت أحد أحفاده).
  - اختبار: إنشاء صنف رئيسي وفرعي عبر API، `GET /categories/tree` رجع الشجرة متداخلة وصحيحة، محاولة حذف صنف له أبناء رُفضت (409).
- [x] Module `attributes` + `attribute_options` + `category_attributes` (نوع الصفة، وحدتها، هل تُفلتر، هل تُنشئ متغير).
  - اختبار: إنشاء صفة "اللون" (`COLOR_SELECT`) مع خيار "أحمر"، ربطها بصنف فرعي مع `createsVariant: true`، وجلبها عبر `GET /category-attributes?categoryId=...` — كل شي رجع صحيح. محاولة حذف صفة مرتبطة بصنف رُفضت (409).
- [x] Audit log أساسي مربوط بكل العمليات الحساسة (`auth.*`, `category.*`, `attribute.*`, `category_attribute.*`) — سجل غير قابل للتعديل من التطبيق (إنشاء فقط).
  - اختبار: راجعنا جدول `audit_logs` فعليًا بعد كل العمليات أعلاه، وكل سطر موجود بالفاعل والوقت والقيم قبل/بعد.

**ملاحظات من التنفيذ الفعلي (تستحق التوثيق):**
- ثغرة أمنية حقيقية اكتُشفت وصُلّحت أثناء البناء: `forgot-password` كان مخطط له يرجّع رمز الاستعادة مباشرة بالـ API response (كنا رح نعتمد عليه لعدم وجود بنية بريد جاهزة) — هذا كان سيسمح لأي شخص يعرف إيميل حساب أنه يستولي عليه فورًا. بنينا `MailService` (nodemailer + Mailpit) بدل هيك.
- تدوير الـ Refresh Token يشمل **كشف إعادة الاستخدام** (reuse detection): لو حدا استخدم refresh token متجاوَز (rotated) من قبل، بيتم إلغاء *كل* جلسات المستخدم فورًا — هاي آلية معيارية لرصد سرقة التوكن.
- تسجيل المستخدم الجديد يخليه `ACTIVE` فورًا بدون تحقق بريد/هاتف فعلي — التحقق (email/phone verification) مؤجل لحد ما تُبنى وحدة الإشعارات (Phase 4)، موثّق كتبسيط مقصود.
- إعادة تكرار مشكلة `npm ci` على Windows بسبب اعتماديات WASM اختيارية (`@node-rs/argon2`) — نفس الحل: حذف `node_modules` + `package-lock.json` وإعادة التثبيت من الصفر.
- عطّلنا `"incremental": true` بـ `tsconfig.json` نهائيًا (بدل محاولة تنظيف `.tsbuildinfo` يدويًا كل مرة) لأن الكاش كان يتلوث بشكل متكرر بين بناء Windows (host) وLinux (container) عبر الـ bind mount، وهاد سبب فشل صامت (JS files ما كانت تتولد) أكثر من مرة.
- 26 صلاحية دقيقة معرّفة بـ `src/common/constants/permissions.constants.ts` كمصدر وحيد للحقيقة (يُستخدم بالـ seed وبالـ Guards معًا).

## Phase 2 — الكتالوج: المنتجات، المتغيرات، الأسعار، البحث، الاستيراد (جزئيًا مكتملة ✅ — الاستيراد والـ cache لسا)

- [x] Module `brands`: CRUD بسيط، منع حذف علامة تجارية لها منتجات.
- [x] Module `products`: منتج بسيط (Product + متغير ضمني واحد تلقائيًا) + منتج متغير (Product + ProductVariant[])، مع التحقق من صفات الصنف المعلوماتية الإلزامية.
  - اختبار فعلي: أُنشئ منتج بسيط (`SIMPLE`) بمتغير ضمني وSKU مباشرة، ومنتج متغير (`VARIABLE`) بصنف له صفة "اللون"، وتم جلبهما عبر `GET /products/slug/:slug` بعد النشر — كل شي صحيح.
- [x] Module `variants`: توليد تركيبات الصفات مع `combinationKey` محسوب (sorted attributeId:value)، منع تكرار التركيبة، SKU فريد على مستوى النظام، منع حذف آخر متغير متبقي للمنتج.
  - اختبار فعلي: إنشاء متغيرين بنفس تركيبة الصفات (لون=أحمر) رجع 409 برسالة دقيقة، وقيمة صفة غير موجودة بخيارات الصفة (لون=بنفسجي) رجعت 400، وSKU مكرر بين منتجين مختلفين رجع 409.
- [x] Module `pricing`: أسعار تجزئة/جملة لكل متغير (`price_lists` مزروعة بـ retail/wholesale)، `POST /variants/:id/prices` وتحديث جماعي `POST /prices/bulk`.
  - اختبار فعلي: تعيين سعرين مختلفين لمتغيرين، وتحديث جماعي لسعر الجملة لمتغيرين معًا بطلب واحد — كل شي انحفظ صح.
- [x] السعر المعروض للمنتج المتغير = نطاق سعر (min/max) محسوب من أسعار التجزئة لكل المتغيرات وقت الطلب، غير مخزّن.
  - اختبار فعلي: منتج بمتغيرين (20 و25) رجع `displayPrice: {min:20, max:25}` صحيح.
- [x] البحث والفلترة: GIN/pg_trgm index حقيقي على `products.name` (migration SQL يدوي)، فلترة بالصفات (`?attr_color=red`)، نطاق السعر (`?minPrice&maxPrice`)، الصنف، العلامة التجارية، وPagination cursor-based.
  - اختبار فعلي: بحث نصي (`?q=Transfer`)، فلترة صفة لون=أحمر رجعت المنتج الصحيح فقط ولون=بنفسجي رجعت صفر نتائج، فلترة نطاق سعر مشترك (`minPrice=15&maxPrice=22`).
  - **باگ حقيقي اكتُشف وصُلّح بالاختبار:** عند دمج `minPrice` و`maxPrice` معًا كانا يتشاركا نفس مفتاح `amount` بكائن JS واحد (`{...{gte:X}, ...{lte:Y}}`) فيلغي الثاني الأول بالكامل (spread لا يدمج المفاتيح المتداخلة) — فكان الفلتر الأدنى يُتجاهل صامتًا. تم إصلاحه بدمج `gte`/`lte` داخل كائن `amount` واحد.
- [ ] Module `media`: رفع صور عبر presigned URL إلى MinIO، فحص MIME/الحجم/الامتداد، إعادة تسمية بمعرفات عشوائية — **لم يُبنَ بعد**.
- [ ] Module `imports` (Import Staging) — **لم يُبنَ بعد**.
- [ ] Cache (Redis) لقوائم الأصناف/المنتجات الأكثر طلبًا + invalidation — **مؤجّل**، لا داعي له قبل وجود بيانات حقيقية بحجم يستدعيه.

**ملاحظات من التنفيذ الفعلي:**
- كل منتج (بسيط أو متغير) له دائمًا `ProductVariant` واحد على الأقل يحمل SKU/السعر/الوزن — المنتج البسيط هو حالة خاصة من المتغير (متغير واحد بـ `combinationKey` فارغ)، بدل بناء نموذجين منفصلين للسعر والمخزون. هذا قرار تصميم مقصود لتفادي ازدواجية المنطق.
- إضافة قيم صفات على منتج/متغير تُتحقق آليًا مقابل تعريف الصفة بالصنف: نوع القيمة (رقم/خيار من قائمة/نص)، هل الصفة إلزامية، وهل هي صفة "منشئة لمتغير" أم "معلوماتية فقط" — لا يمكن إرسال قيمة لصفة لا تخص الصنف.
- `docker compose exec api npx prisma migrate dev` (بدون `--name`) يفتح prompt تفاعلي أحيانًا حتى بعد نجاح الـ migration — استخدم `--name` دائمًا، أو `--create-only` ثم migrate dev منفصل عند كتابة SQL يدوي.
- إصلاح Dockerfile: مرحلة `development` ما كانت تعمل `chown` لـ `node_modules` (بعكس مرحلة `production`)، فكان `prisma generate` يفشل بصلاحيات داخل الحاوية لأن العملية تعمل بمستخدم `node` غير المالك. أُضيف `RUN chown -R node:node /app` لمرحلة `development` أيضًا.
- ملاحظة تشغيلية: كتابة ملفات/مجلدات جديدة كثيرة دفعة واحدة (وحدات كاملة) أحيانًا لا يلتقطها file-watcher الخاص بـ `nest start --watch` داخل Docker على Windows تلقائيًا؛ إعادة تشغيل الحاوية (`docker compose restart api`) كانت الحل الموثوق بعد إضافة وحدات جديدة كاملة.

## Phase 3 — التجارة: السلة، المخزون، الطلبات، الشحن، الدفع

- [ ] Module `warehouses` + `inventory_items` (رصيد متاح ومحجوز لكل متغير/مستودع).
- [ ] Module `inventory` — حركات (`inventory_movements`): إضافة، خصم، حجز، تحرير، تسوية، مع Transaction + قفل مناسب لمنع overselling.
  - اختبار حاسم: **اختبار race condition** — طلبين متزامنين على آخر قطعة بالمخزون، والتأكد أن واحد فقط ينجح.
- [ ] Module `carts` / `cart_items`.
  - اختبار: إضافة/تعديل/حذف عناصر السلة فعليًا عبر API.
- [ ] Module `orders`: دورة الحالات الكاملة (DRAFT → ... → DELIVERED + الحالات الاستثنائية)، Snapshot لبيانات المنتج عند إنشاء الطلب.
  - اختبار: إنشاء طلب ثم تعديل سعر المنتج بالكتالوج، والتأكد أن الطلب القديم يحتفظ بالسعر الأصلي (Snapshot).
- [ ] منع إتمام طلب بكمية تفوق المخزون المتاح (معيار قبول أساسي بالتوصيف).
  - اختبار: محاولة طلب كمية أكبر من المتاح يجب أن تُرفض بوضوح.
- [ ] Idempotency keys لإنشاء الطلب والدفع.
  - اختبار: إرسال نفس طلب الإنشاء مرتين بنفس idempotency key لا يُنشئ طلبين.
- [ ] تسجيل كل انتقال حالة (الفاعل، التاريخ، السبب).
- [ ] Module `payments/refunds` (تكامل بوابة دفع تجريبية أو mock بالبداية حسب القرار المطلوب بالأعلى).
- [ ] Module `shipping/shipments` (تتبع أساسي).
- [ ] Jobs (BullMQ): `stock-release.job` (تحرير حجز عند إلغاء/انتهاء مهلة دفع)، `abandoned-cart.job`.
  - اختبار: إنشاء طلب وعدم دفعه، والتأكد أن الـ job يحرر الحجز فعليًا بعد المهلة.

## Phase 4 — الإدارة والتقارير

- [ ] لوحة مؤشرات أساسية (تقارير مبيعات، مخزون منخفض، منتجات راكدة) — API فقط بهاي المرحلة.
- [ ] استكمال `audit_logs` لتغطية الأسعار والمخزون والاسترجاعات والصلاحيات (سجل غير قابل للتعديل).
- [ ] Module `promotions` (كوبونات وعروض).
- [ ] أسعار خاصة لعملاء/شرائح (Sales Agent role).
- [ ] Module `notifications` (بريد على الأقل، عبر mailpit بالتطوير).
- [ ] اختبارات صلاحيات شاملة لكل دور (Super Admin, Catalog Manager, Inventory Manager, Order Manager, Sales Agent, Customer).
  - اختبار: مصفوفة اختبارات فعلية — كل دور يُجرب على endpoints لا يملك صلاحيتها ويجب أن يُرفض.

## Phase 5 — التشغيل والجاهزية للإنتاج

- [ ] Testcontainers لاختبارات Integration مع Postgres/Redis حقيقيين (لا mocks).
- [ ] E2E tests للمسارات الحرجة: تسجيل، سلة، طلب، خصم مخزون، إلغاء.
- [ ] فحص ثغرات الحزم والصورة (`npm audit` / Trivy أو ما يعادله) داخل CI.
- [ ] CI/CD: lint + type-check + unit/integration tests + build image + migrations + smoke tests + موافقة يدوية → production.
- [ ] Prometheus metrics + tracing (OpenTelemetry) + تنبيهات (5xx مرتفع، مخزون منخفض، فشل backup، توقف worker).
- [ ] نسخ احتياطية مشفرة + اختبار استعادة فعلي دوري (تجربة استعادة حقيقية لا افتراضية).
- [ ] توثيق OpenAPI/Swagger كامل لكل الـ endpoints.
- [ ] مراجعة أمنية أخيرة مقابل قائمة القسم 14 بالتوصيف (Helmet, secrets, rate limiting, RBAC, presigned URLs...) قبل الإطلاق.

---

## معايير القبول النهائية (من التوصيف، للمراجعة المستمرة)

- [ ] إنشاء صنف جديد وربطه بصفات جديدة دون تعديل شيفرة.
- [ ] إنشاء منتج متغير وتوليد تركيباته مع منع التكرار.
- [ ] كل متغير له سعر ومخزون وSKU مستقل.
- [ ] فلترة المنتجات حسب صفات الصنف من واجهة العميل.
- [ ] استحالة إتمام طلب بكمية تفوق المخزون المتاح.
- [ ] كل حركة مخزون وتعديل سعر مسجل وقابل للتتبع.
- [ ] صلاحيات الإدارة مفصولة ومختبرة فعليًا.
- [ ] كل الخدمات تعمل بالكامل عبر Docker Compose بالتطوير.
- [ ] توثيق OpenAPI واختبارات للمسارات الحرجة موجودة وتعمل.

---

## ملاحظة حول الأمن (لأن هاد مشروع حقيقي)

الأمن مو مرحلة أخيرة — كل بند بقسم "14. الحماية والأمن" بالتوصيف (Argon2id، RBAC دقيق، rate limiting، Helmet، presigned URLs، عدم تسجيل الأسرار، فصل الأسرار عن صورة Docker...) يُطبّق **أثناء** بناء الميزة المرتبطة فيه، مو بعدها. كل مهمة أعلاه فيها بند أمني ضمني يجب التحقق منه وقت الاختبار المحلي.
