# خطة تنفيذ مشروع متجر مواد الطباعة (NestJS + Docker)

مصدر التوصيف: `توصيف_مشروع_متجر_مواد_الطباعة_NestJS_Docker.docx`

## القاعدة الذهبية للتنفيذ

**كل مهمة تُنفذ يجب أن:**
1. تُبنى بشكل كامل (كود + migration إن وجد + test).
2. تُشغَّل محليًا عبر `docker compose up` (أو الخدمة المعنية) للتأكد أنها تعمل فعليًا، مش بس compile.
3. تُختبر يدويًا (طلب API حقيقي عبر curl/Postman/Swagger) + `npm run test` / `test:e2e` حسب الحالة.
4. لا تُعتبر "خلصانة" ولا ننتقل للمهمة التالية إلا بعد التحقق الفعلي أنها شغالة (لا كود بدون تجربة).
5. تُعمل commit مستقل بعد كل مهمة مكتملة ومُختبرة (رسائل commit واضحة).

---

## قرارات يجب حسمها قبل البدء (من القسم 25 بالتوصيف)

هاي القرارات بتأثر على تصميم قاعدة البيانات من أول فيز، لازم نحسمها قبل أو خلال Phase 1:

- [ ] هل الأسعار تشمل الضريبة أم تُضاف لاحقًا؟
- [ ] هل البيع للأفراد والجملة معًا (نفس الحساب) أم منفصل؟
- [ ] هل المخزون موزع على أكثر من مستودع من البداية؟
- [ ] متى يُحجز المخزون: عند إضافة للسلة، عند إنشاء الطلب، أم عند نجاح الدفع؟
- [ ] هل يُسمح بالبيع بأجزاء من المتر/الرول (كميات عشرية)؟
- [ ] ما بوابات الدفع وشركات الشحن المستهدفة فعليًا؟
- [ ] هل السعر موحّد أم يختلف حسب مجموعة العميل/الفرع؟
- [ ] هل يوجد شحن فقط أم استلام من الفرع أيضًا؟

> سنبدأ بافتراضات معقولة (مستودع واحد، حجز عند تأكيد الطلب، دون ضريبة منفصلة الآن) ونعدلها متى تم الحسم، لكن الأفضل حسمها بأقرب وقت.

---

## Phase 0 — تهيئة البنية التحتية والأمن الأساسي ✅ (مكتملة ومُختبرة محليًا)

- [x] إعداد `docker-compose.yml`: postgres, redis, minio, mailpit (dev فقط), nginx.
  - اختبار: `docker compose up -d` — كل الخدمات الست شغالة (`postgres`, `redis` healthy فعليًا؛ `minio`, `mailpit`, `nginx`, `api` تم التحقق من استجابتها عبر curl).
- [x] Dockerfile متعدد المراحل (base/deps/prod-deps/development/build/production) + مستخدم non-root (`node`) + `.dockerignore`.
  - اختبار: بُنيت صورة `production` وشُغّلت فعليًا متصلة بقاعدة بيانات حقيقية، healthcheck ظهر `healthy`، و`whoami` داخل الحاوية أكّد أنها تعمل بمستخدم `node` لا `root`.
- [x] إعداد Prisma 7 (schema + `prisma.config.ts` + `@prisma/adapter-pg`) + اتصال بقاعدة postgres عبر docker.
  - اختبار: `PrismaService` اتصل فعليًا بالقاعدة (ظهر بالـ logs "Connected to database")، و`/health/ready` أكّد الاتصال حيًا. توليد جداول حقيقية سيُختبر مع أول migration بـ Phase 1.
- [x] `config/` مع `validation.schema.ts` (Joi) لمنع الإقلاع بدون متغيرات بيئة صحيحة.
- [x] Helmet, CORS allow-list, تعطيل X-Powered-By, ValidationPipe (whitelist + forbidNonWhitelisted + transform) عام على مستوى `main.ts`.
  - اختبار: تأكدنا من ظهور رؤوس Helmet الأمنية فعليًا بالاستجابة (CSP, HSTS, X-Frame-Options...) وغياب X-Powered-By. اختبار forbidNonWhitelisted الكامل هيصير مع أول DTO حقيقي بـ Phase 1.
- [x] `/health/live` و`/health/ready` (فحص اتصال DB/Redis فعليًا عبر Terminus).
  - اختبار: `curl` مباشر وعبر nginx reverse proxy رجعوا 200 مع تفاصيل صحة DB وRedis.
- [x] Pino structured logging (JSON بالإنتاج، pretty بالتطوير، redact لكلمات المرور والتوكنات).
  - اختبار: راجعنا سطور log فعلية (dev وproduction)، ولقينا وصلّحنا باگ حقيقي: كانت الصورة رح تنهار بالإنتاج لو NODE_ENV اتضبط غلط لأن `pino-pretty` (devDependency) مو موجودة بصورة الإنتاج — تم عمل fallback آمن.

**ملاحظات من التنفيذ الفعلي (تستحق التوثيق):**
- Prisma 7 غيّر الـ API: الاتصال بقاعدة البيانات صار عبر `adapter` (`@prisma/adapter-pg`) مو عبر `datasource.url` بالـ schema مباشرة.
- تم تصحيح `tsconfig.build.json` (`rootDir: src` + استثناء `prisma.config.ts`) لأن وجود `prisma.config.ts` بجذر المشروع كان يغيّر مسار الـ build الناتج (`dist/src/main.js` بدل `dist/main.js`).
- ملف `*.tsbuildinfo` تمت إضافته لـ `.gitignore` و`.dockerignore` لأنه كان يسبب cache قديم فاسد داخل صورة Docker.
- خدمة `worker` (BullMQ) لم تُضف بعد لـ `docker-compose.yml` لأنه لا توجد jobs فعلية حتى الآن — ستُضاف مع Phase 3.

## Phase 1 — الأساس: الهوية، RBAC، الأصناف والصفات

- [ ] Module `users`: تسجيل، تسجيل دخول، تشفير كلمة المرور بـ Argon2id.
  - اختبار: تسجيل مستخدم فعلي عبر Postman، التأكد أن الباسورد بقاعدة البيانات مشفر لا نص صريح.
- [ ] JWT Access Token (قصير العمر) + Refresh Token (مخزّن مجزأ/hashed بقاعدة البيانات) مع تدوير (rotation).
  - اختبار: تسجيل دخول → refresh → التأكد أن الـ refresh القديم يصير غير صالح (rotation فعلي).
- [ ] إلغاء جلسة واحدة / كل الجلسات + تسجيل device/IP لكل جلسة.
  - اختبار: تسجيل جلستين، إلغاء واحدة، التأكد أن الثانية ما زالت تعمل والأولى لأ.
- [ ] Rate limiting (عام + مشدد على `/auth/login`, `/auth/forgot-password`).
  - اختبار: إرسال طلبات متتالية سريعة والتأكد من 429.
- [ ] Guards + RBAC (roles + permissions دقيقة: `products.create`, `prices.update`, `orders.refund` ...).
  - اختبار: مستخدم بدون صلاحية يحصل 403 فعليًا عبر endpoint محمي تجريبي.
- [ ] MFA لحسابات الإدارة (اختياري بالمرحلة الأولى لكن يُبنى الأساس/interface له).
- [ ] استعادة كلمة المرور: رمز أحادي الاستخدام محدد الصلاحية.
  - اختبار: استخدام نفس رمز الاستعادة مرتين يجب أن يفشل بالمرة الثانية.
- [ ] Module `categories`: شجرة أصناف متعددة المستويات (parent_id + path)، CRUD كامل من لوحة الإدارة دون تعديل شيفرة.
  - اختبار: إنشاء صنف رئيسي وفرعي عبر API والتأكد من الشجرة صح بقاعدة البيانات.
- [ ] Module `attributes` + `attribute_options` + `category_attributes` (نوع الصفة، وحدتها، هل تُفلتر، هل تُنشئ متغير).
  - اختبار: ربط صفة (مثلاً السماكة) بصنف (بلكسي) والتأكد من ظهورها عبر API الخاص بالصنف.
- [ ] Audit log أساسي (من الآن) لأي تعديل حساس (users, roles, categories, attributes).
  - اختبار: تعديل صنف والتأكد من ظهور سطر بجدول `audit_logs`.

## Phase 2 — الكتالوج: المنتجات، المتغيرات، الأسعار، البحث، الاستيراد

- [ ] Module `products`: منتج بسيط (سعر ومخزون واحد) + منتج متغير (Product + ProductVariant[]).
  - اختبار: إنشاء منتج بسيط ومنتج متغير فعليًا وجلبهما عبر `GET /products/:slug`.
- [ ] Module `variants`: توليد تركيبات الصفات، منع تكرار التركيبة، SKU فريد على مستوى النظام.
  - اختبار: محاولة إنشاء متغيرين بنفس تركيبة الصفات يجب أن تُرفض.
- [ ] Module `media`: رفع صور عبر presigned URL إلى MinIO، فحص MIME/الحجم/الامتداد، إعادة تسمية بمعرفات عشوائية.
  - اختبار: رفع صورة فعلي عبر presign URL محليًا والتأكد من وصولها لـ MinIO.
- [ ] Module `pricing`: تجزئة/جملة/شرائح عملاء، تحديث جماعي.
  - اختبار: تعيين سعرين مختلفين (تجزئة/جملة) لنفس المتغير والتأكد من إرجاع السعر الصحيح حسب نوع الحساب.
- [ ] السعر المعروض للمنتج المتغير = أدنى سعر أو نطاق سعر (منطق محسوب لا مخزّن).
  - اختبار: مقارنة السعر المعروض بالحساب اليدوي لأدنى سعر بين المتغيرات.
- [ ] البحث والفلترة: فهرسة على slug/SKU/barcode/category_id/status + GIN/pg_trgm للنص العربي، فلترة بالصفات ونطاق السعر والعلامة التجارية والتوفر.
  - اختبار: بحث نصي بالعربي فعليًا يرجع نتائج صحيحة، وفلترة صفة (لون=أحمر) ترجع فقط المطابق.
- [ ] Pagination cursor-based للقوائم الكبيرة.
- [ ] Module `imports` (Import Staging): رفع Excel/CSV، خريطة أعمدة، تنظيف قيم، مطابقة أصناف/صفات، كشف SKU مكرر، Preview + تقرير أخطاء، اعتماد ضمن Transaction، حفظ نتيجة كل صف.
  - اختبار: استيراد ملف تجريبي فيه صف خاطئ وصف مكرر SKU، والتأكد أن الـ Preview يُظهر الأخطاء دون إدخالها فعليًا لقاعدة البيانات قبل الاعتماد.
- [ ] Cache (Redis) لقوائم الأصناف/المنتجات الأكثر طلبًا + invalidation عند التعديل.
  - اختبار: تعديل منتج والتأكد أن الـ cache انعكس عليه فورًا (لا بيانات قديمة).

## Phase 3 — التجارة: السلة، المخزون، الطلبات، الشحن، الدفع

- [ ] Module `warehouses` + `inventory_items` (رصيد متاح ومحجوز لكل متغير/مستودع).
- [ ] Module `inventory` — حركات (`inventory_movements`): إضافة، خصم، حجز، تحرير، تسوية، مع Transaction + قفل مناسب لمنع overselling.
  - اختبار حاسم: **اختبار race condition** — طلبين متزامنين على آخر قطعة بالمخزون، والتأكد أن واحد فقط ينجح.
- [ ] Module `carts` / `cart_items`.
  - اختبار: إضافة/تعديل/حذف عناصر السلة فعليًا عبر API.
- [ ] Module `orders`: دورة الحالات الكاملة (DRAFT → ... → DELIVERED + الحالات الاستثنائية)، Snapshot لبيانات المنتج عند إنشاء الطلب.
  - اختبار: إنشاء طلب ثم تعديل سعر المنتج بالكتالوج، والتأكد أن الطلب القديم يحتفظ بالسعر الأصلي (Snapshot).
- [ ] منع إتمام طلب بكمية تفوق المخزون المتاح (معيار قبول أساسي بالتوصيف).
  - اختبار: محاولة طلب كمية أكبر من المتاح يجب أن تُرفض بوضوح.
- [ ] Idempotency keys لإنشاء الطلب والدفع.
  - اختبار: إرسال نفس طلب الإنشاء مرتين بنفس idempotency key لا يُنشئ طلبين.
- [ ] تسجيل كل انتقال حالة (الفاعل، التاريخ، السبب).
- [ ] Module `payments/refunds` (تكامل بوابة دفع تجريبية أو mock بالبداية حسب القرار المطلوب بالأعلى).
- [ ] Module `shipping/shipments` (تتبع أساسي).
- [ ] Jobs (BullMQ): `stock-release.job` (تحرير حجز عند إلغاء/انتهاء مهلة دفع)، `abandoned-cart.job`.
  - اختبار: إنشاء طلب وعدم دفعه، والتأكد أن الـ job يحرر الحجز فعليًا بعد المهلة.

## Phase 4 — الإدارة والتقارير

- [ ] لوحة مؤشرات أساسية (تقارير مبيعات، مخزون منخفض، منتجات راكدة) — API فقط بهاي المرحلة.
- [ ] استكمال `audit_logs` لتغطية الأسعار والمخزون والاسترجاعات والصلاحيات (سجل غير قابل للتعديل).
- [ ] Module `promotions` (كوبونات وعروض).
- [ ] أسعار خاصة لعملاء/شرائح (Sales Agent role).
- [ ] Module `notifications` (بريد على الأقل، عبر mailpit بالتطوير).
- [ ] اختبارات صلاحيات شاملة لكل دور (Super Admin, Catalog Manager, Inventory Manager, Order Manager, Sales Agent, Customer).
  - اختبار: مصفوفة اختبارات فعلية — كل دور يُجرب على endpoints لا يملك صلاحيتها ويجب أن يُرفض.

## Phase 5 — التشغيل والجاهزية للإنتاج

- [ ] Testcontainers لاختبارات Integration مع Postgres/Redis حقيقيين (لا mocks).
- [ ] E2E tests للمسارات الحرجة: تسجيل، سلة، طلب، خصم مخزون، إلغاء.
- [ ] فحص ثغرات الحزم والصورة (`npm audit` / Trivy أو ما يعادله) داخل CI.
- [ ] CI/CD: lint + type-check + unit/integration tests + build image + migrations + smoke tests + موافقة يدوية → production.
- [ ] Prometheus metrics + tracing (OpenTelemetry) + تنبيهات (5xx مرتفع، مخزون منخفض، فشل backup، توقف worker).
- [ ] نسخ احتياطية مشفرة + اختبار استعادة فعلي دوري (تجربة استعادة حقيقية لا افتراضية).
- [ ] توثيق OpenAPI/Swagger كامل لكل الـ endpoints.
- [ ] مراجعة أمنية أخيرة مقابل قائمة القسم 14 بالتوصيف (Helmet, secrets, rate limiting, RBAC, presigned URLs...) قبل الإطلاق.

---

## معايير القبول النهائية (من التوصيف، للمراجعة المستمرة)

- [ ] إنشاء صنف جديد وربطه بصفات جديدة دون تعديل شيفرة.
- [ ] إنشاء منتج متغير وتوليد تركيباته مع منع التكرار.
- [ ] كل متغير له سعر ومخزون وSKU مستقل.
- [ ] فلترة المنتجات حسب صفات الصنف من واجهة العميل.
- [ ] استحالة إتمام طلب بكمية تفوق المخزون المتاح.
- [ ] كل حركة مخزون وتعديل سعر مسجل وقابل للتتبع.
- [ ] صلاحيات الإدارة مفصولة ومختبرة فعليًا.
- [ ] كل الخدمات تعمل بالكامل عبر Docker Compose بالتطوير.
- [ ] توثيق OpenAPI واختبارات للمسارات الحرجة موجودة وتعمل.

---

## ملاحظة حول الأمن (لأن هاد مشروع حقيقي)

الأمن مو مرحلة أخيرة — كل بند بقسم "14. الحماية والأمن" بالتوصيف (Argon2id، RBAC دقيق، rate limiting، Helmet، presigned URLs، عدم تسجيل الأسرار، فصل الأسرار عن صورة Docker...) يُطبّق **أثناء** بناء الميزة المرتبطة فيه، مو بعدها. كل مهمة أعلاه فيها بند أمني ضمني يجب التحقق منه وقت الاختبار المحلي.
