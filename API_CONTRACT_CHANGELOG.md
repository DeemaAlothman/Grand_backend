# سجل تغييرات العقد (API_CONTRACT.md)

ملف منفصل يوثّق كل تصحيح/إضافة صارت على العقد وليش، بدل ما تضيع بالتاريخ داخل `API_CONTRACT.md` نفسه. كل دفعة تغييرات إلها تاريخ وسبب.

---

## 2026-08-02 — مراجعة عقد شاملة (قبل استكمال Phase 4)

السبب: فريق الفرونت راجع `API_CONTRACT.md` بالتفصيل قبل ما يبلش يبني عليه، ولقى تناقض حقيقي وكم فجوة. هاي حصيلة المراجعة والإجراء المتخذ لكل نقطة.

### 🐞 خلل حقيقي انصلح

- **تناقض إلغاء الطلب:** التوثيق كان بيقول للفرونت "استخدم `PATCH /orders/:id/status` للإلغاء" بينما نفس المسار كان يتطلب صلاحية `orders.updateStatus` والعميل صلاحياته فاضية — يعني الزبون فعليًا ما كان يقدر يلغي طلبه بنفسه رغم إن التوثيق بيقول إله يقدر. **الحل:** العميل صاحب الطلب هلأ يقدر يستخدم نفس المسار للإلغاء فقط (بدون أي صلاحية)، وأي انتقال حالة تاني من عميل عادي برجع `403`. غير المالك بدون صلاحية برجع `404` (نفس منطق إخفاء الوجود المستخدم بمكان تاني بالنظام). شوف `authorizeStatusChange` بـ`orders.service.ts`.

### ➕ Endpoints جديدة (فجوات حقيقية اتسدّت)

| Endpoint | السبب |
|---|---|
| `GET /products/admin` | منتج `DRAFT`/`ARCHIVED` ما كان ظاهر بأي قائمة — بس بمعرفة الـ id مسبقًا. هلأ عنده قائمة إدارية بكل الحالات + فلترة `status`. |
| `GET /orders?status=&cursor=&limit=` | `GET /orders` كانت بترجع كل الطلبات بدون حدود — مشكلة أداء أكيدة مع نمو البيانات. أضفنا نفس نمط pagination المستخدم بـ`/products`. |
| `PATCH /warehouses/:id` | ما كان في طريقة لتعديل اسم مستودع أو تعطيله. أضفنا `PATCH` بحقلي `name`/`isActive` فقط — **بدون حذف فعلي** (قرار متعمد، شوف تحت). |
| حقل `inStock` على `/products` و`/products/slug/:slug` | ما كان في طريقة يعرف فيها الزبون "غير متوفر" قبل ما يوصل لخطوة الدفع ويطلع 409. أضفنا `true`/`false` بس (بدون أرقام) على مستوى المنتج وكل متغير. |

### ✅ نقاط بالمراجعة تبيّن إنها مو فجوة فعليًا (كانت موجودة أصلًا، بس التوثيق ناقص)

- **جلب `paymentId` للاسترجاع:** `GET /orders/:id` كان أصلًا بيرجّع `payments[]` كاملة ضمن رد الطلب (من Phase 3) — التوثيق بس ما كان يذكر هاد صراحة. تم توضيحه بالعقد.
- **تتبع الشحنة للزبون:** نفس الشي، `shipments[]` (فيها `trackingNumber`) موجودة أصلًا برد `GET /orders/:id`، والزبون صاحب الطلب يشوفها عادي. الـ endpoint المنفصل `/orders/:orderId/shipments` إداري فقط بس هذا مقصود.
- **استعلام مخزون منخفض:** كان مطلوب بالمراجعة، بس تبيّن إنه انبنى أصلًا ضمن نفس Phase 4 الجارية (`GET /reports/low-stock`) — بس التوثيق ما كان تحدّث لسا. تمت إضافة قسم Reports كامل للعقد (كان ناقص كليًا، مو بس هاد الجزء — شوف تحت).

### 📄 أقسام كانت ناقصة كليًا من العقد

قسم **Reports** (`/reports/sales`, `/reports/low-stock`, `/reports/stagnant-products`) وقسم **Promotions/Coupons** (`/coupons/*`) كانوا مبنيين ومُختبرين ضمن Phase 4 الجارية بس ما انضافوا للعقد أبدًا. تمت إضافتهم كاملين.

### 📝 توضيحات وثّقناها بالعقد (سلوك موجود، بس ما كان مكتوب)

- **العملة:** `USD` ثابتة لكل النظام، رقمين عشريين (`Decimal(12,2)`) — لا يوجد دعم متعدد عملات حاليًا.
- **فك ربط صفة عن صنف (`DELETE /category-attributes/:categoryId/:attributeId`):** عملية آمنة — بتحذف الربط بس **ما بتلمس** قيم الصفة الموجودة على منتجات/متغيرات موجودة مسبقًا.
- **`couponCode`** على `POST /orders`، وربط قائمة أسعار خاصة بعميل عبر `PATCH /customers/:customerId/price-list` (وكيف بينعكس على `unitPriceSnapshot`).
- **إشعارات بريدية تلقائية** (إنشاء طلب / استلام دفع / شحن / تسليم) — تصير تلقائيًا بدون أي استدعاء من الفرونت.

### 🔧 قرارات منتج اتخذت بهالجلسة

| القرار | الخيار المعتمد |
|---|---|
| حذف مستودع | **تعطيل فقط (`isActive: false`)، بدون حذف فعلي.** حذف مستودع فيه حركات مخزون فعلية خطير على البيانات التاريخية. `409` لو حاولت تعطّل آخر مستودع فعّال بالنظام. |
| ظهور التوفر للزبون قبل إتمام الطلب | **`true`/`false` بس.** لا كشف لأرقام المخزون الفعلية بأي رد عام — الأرقام محصورة بـ`/inventory` و`/reports/low-stock` (صلاحيات إدارية). |
| تفعيل التحقق من البريد الإلكتروني عند التسجيل | **يبقى معطّل حاليًا**، يُعاد النظر فيه ضمن Phase 5 (الجاهزية التشغيلية). |

### 🛠️ أداة جديدة

**`npm run verify:contract`** (أو `VERIFY_WRITES=1 npm run verify:contract` لفحص أعمق يشمل الكتابة الفعلية). بيتصل بالسيرفر المحلي الشغّال ويتحقق آليًا من أشكال الردود الحقيقية بدل ما حدا يجاوب هالأسئلة يدويًا في كل مرة — كان أغلب "الأسئلة القابلة للفحص الآلي" اللي طرحها الفرونت ممكن يجاوبها هالسكربت لحاله بدقيقتين. الكود بـ`scripts/verify-contract.ts`.

### الملفات المتأثرة (باختصار)

`orders.controller.ts`, `orders.service.ts` (تفويض الإلغاء) · `products.controller.ts`, `products.service.ts` (قائمة إدارية + `inStock`) · `warehouses.controller.ts`, `warehouses.service.ts`, `dto/update-warehouse.dto.ts` (PATCH) · `scripts/verify-contract.ts`, `package.json` (سكربت جديد) · `tsconfig.json`, `tsconfig.build.json` (استثناء `scripts/` من بناء Nest) · `API_CONTRACT.md` (كل التصحيحات فوق).
